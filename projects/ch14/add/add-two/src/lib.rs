//! # Add Two
//!
//! `add_two` contains a function that adds two to a give number.

/// Adds two to the number given
///
/// # Examples
/// ```
/// let arg = 5;
/// let answer = add_two::add_two(arg);
///
/// assert_eq!(7, answer);
/// ```
pub fn add_two(x: i32) -> i32 {
    x + 2
}
